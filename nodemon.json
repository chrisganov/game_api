{
  "watch": ["src", ".env"],
  "ext": "js,ts,json",
  "ignore": ["src/logs/*", "src/**/*.{spec,test}.ts"],
  "exec": "ts-node-dev -r tsconfig-paths/register --files --transpile-only --respawn --inspect --project tsconfig.json src/index.ts"
}
